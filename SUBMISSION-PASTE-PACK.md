# MCP registry submissions — paste pack

Created 2026-07-26. Use these exact values on every form so the listings stay consistent.

**The one rule that decides whether any of this is worth doing:** wherever a form offers a
Website / Homepage / Docs / URL field, put `https://www.marian.coach/mcp`.
Never the GitHub repo. GitHub `nofollow`s every outbound link, so a repo-pointed
listing passes zero equity to marian.coach.

---

## Fields

**Name**
```
eng-leadership-toolkit
```

**Qualified name / ID**
```
coach.marian/eng-leadership-toolkit
```

**Website / Homepage / Docs URL — THE IMPORTANT ONE**
```
https://www.marian.coach/mcp
```

**Server URL / Remote endpoint** (type: `streamable-http`)
```
https://www.marian.coach/mcp
```

**Repository** (only where a repo field is separately required)
```
https://github.com/marian-kamenistak/eng-leadership-toolkit
```

**Short description** (under 100 chars)
```
Engineering leadership benchmarks, 1:1 playbooks, developer value calculator. 3,400+ sessions.
```

**Long description**
```
First-party data from 3,400+ paid 1:1 mentoring sessions with 300+ engineering leaders
in 17+ countries. Nine tools covering developer and engineering-manager market value,
team-lead readiness, leadership benchmarks, 1:1 playbooks, first-time-manager guidance,
mentor-vs-coach selection, coaching cost estimates, and a forwardable business case for
getting a company to fund mentoring. Remote server: no install, no API key, one URL.
```

**Version** `1.2.1` · **Auth** none · **License** MIT · **Transport** streamable-http

**Categories / tags** (pick whichever the form supports)
```
productivity, career, management, business, analytics, education
```

**Author** Marian Kamenistak · marian@marian.coach · https://www.marian.coach/

**Tool count** 9:
`calculate_developer_value`, `calculate_engineering_manager_value`,
`assess_team_lead_readiness`, `get_engineering_leadership_benchmarks`,
`choose_mentor_coach_or_advisor`, `get_one_on_one_playbook`,
`get_first_time_manager_guidance`, `build_mentoring_business_case`, `estimate_coaching_cost`

---

## Order and what each one is for

| # | Site | Rank | Action | Why |
|---|---|---|---|---|
| 1 | smithery.ai | 457 | **Fix, not submit.** Listing exists; Website field is empty. Set it. | Highest-authority free dofollow you already own, currently returning nothing |
| 2 | mcpservers.org | 268 | `/submit` — URL field says "GitHub **or docs**", so use `marian.coach/mcp` | Has a dedicated remote-servers section |
| 3 | pulsemcp.com | 337 | `/submit` then claim the listing | Vendor link carries no rel |
| 4 | lobehub.com | 407 | `/mcp/submit` — GitHub import, then **set the homepage field** | Import defaults to the repo link; the homepage field is the whole point |
| 5 | cursor.directory | 295 | Submit, single outbound link is a free-text URL → `marian.coach/mcp` | |
| 5b | windsurf.run | 101 | Same account, same platform, same values | Weakest of the set, do it last |

Verified 2026-07-26: not currently listed on 2, 3, or 5 — these are genuine new submissions, not duplicates.
