{
  "name": "ai-engineering-leader-toolkit",
  "version": "1.5.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "format": "oxfmt --write .",
    "lint:fix": "oxlint --fix",
    "start": "wrangler dev",
    "cf-typegen": "wrangler types",
    "type-check": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "agents": "^0.17.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "oxfmt": "^0.56.0",
    "oxlint": "^1.71.0",
    "typescript": "6.0.3",
    "vitest": "^3.2.7",
    "wrangler": "^4.105.0"
  }
}
